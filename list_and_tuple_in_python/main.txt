

list in python

a built in data type that stores set of values
it can store clements of different typs (integer,float,string,etc)

marks=[94.4,87.5,95.5,66.4,45.1]

print(marks)

string is inmutabel

list is mutable


#list slicing

similar to string slicing

listname[starting_index:end index]

marks=[87,64,33,95,76]
        -5   -4   -3   -2   -1

marks[1:4] is [64,33,95]


#list methods

list=[2,1,3]

list.append(4) #add one element at the end

list.sort()# sorts in ascending order

list.reverse() # reverses list

list.insert(idx,el) #insert element at index

list.remove(1) # remove first occurrence of element

list.pop(idex) #removes element at index
